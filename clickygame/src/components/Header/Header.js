import React from "react";
import "./Header.css";

const Header = () => (
   <header className="header">
   	<h1>Test Your Memory!</h1>
   	<h2>Click on an image to earn points, try not to click the same image twice!</h2>
   </header>

);

export default Header;
